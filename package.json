{
  "name": "bolt-pg",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "bolt": {
    "workspaces": [
      "packages/*"
    ]
  },
  "scripts": {
    "show-the-issue": "bolt ws run run-exposed-cli --only uses-the-cli"
  }
}
